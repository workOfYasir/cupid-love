import React,{useContext} from 'react'
import { Observer } from "mobx-react-lite"



const Terms = () => {

    return (
        <Observer>
            {() => (
                <>
                    phudi k conditions maan
                </>
            )}
        </Observer>
    )
}
export default Terms